{
  "name": "@zenowethu/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:seed:crediva": "ts-node prisma/seed-crediva.ts",
    "db:seed:pilot": "ts-node prisma/seed-real-user.ts",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5"
  },
  "prisma": {
    "schema": "./prisma/schema.prisma"
  }
}
