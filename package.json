{
  "name": "zenowethu",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "turbo": "^2.9.14"
  },
  "pnpm": {
    "overrides": {
      "next-auth": "^5.0.0-beta.30"
    },
    "onlyBuiltDependencies": [
      "@prisma/client",
      "prisma",
      "@prisma/engines",
      "@sentry/cli",
      "sharp",
      "puppeteer",
      "esbuild",
      "@sentry/nextjs",
      "unrs-resolver"
    ]
  }
}